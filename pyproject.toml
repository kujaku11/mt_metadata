[project]
name = "mt-metadata"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "JP", email = "peacock.jared@gmail.com" }
]
requires-python = ">=3.11"
dependencies = []

[project.scripts]
mt-metadata = "mt_metadata:main"

[build-system]
requires = ["uv_build>=0.8.4,<0.9.0"]
build-backend = "uv_build"
